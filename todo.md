# Plim Implementation Checklist

```
opencode -s ses_214d23b71ffeer6YhqE6jqyTem
```

Derived from `REQUIREMENTS.md`. Each item is a hard requirement; the implementation must allow the exact API shown in the requirements file. Items can be extended but not changed.

Legend: `[ ]` pending · `[~]` in progress · `[x]` done.

## 1. Editor (PlimDriver) API — `@plim/core`

- [x] `new PlimDriver({ theme, extensions, registeredMarks, registeredBlocks, registeredActions })`
- [x] `theme` accepts a name (e.g. `'light'`) or a custom theme object
- [x] `extensions` array supported, processed once and cached for reuse across editors
- [x] `registeredMarks` accepts the result of `defineMark(...)` factories
- [x] `registeredBlocks` accepts the result of `defineBlock(...)` factories
- [x] `registeredActions` accepts the result of `defineAction(...)` factories
- [x] `plim.getHistory()` returns the History API (per editor)
- [x] Driver is reusable across multiple `deriveEditor` instances

## 2. Action / Trigger system — `@plim/core`

- [x] `defineAction(name, config)` returns an action descriptor
- [x] `triggers.keyboard.shortcut('Mod+b')` (with `Mod` mapping to ⌘ on macOS, Ctrl elsewhere)
- [x] `triggers.keyboard.character('/')`, `triggers.keyboard.character('@')`, `triggers.keyboard.character(':')`
- [x] `triggers.keyboard.key('Escape')`, `triggers.keyboard.key('Space')`
- [x] `triggers.clipboard.action('cut' | 'copy' | 'paste')`
- [x] Actions can have a single trigger or an array of triggers
- [x] `triggerValidationRules: ({and, or}) => and([...]) | or([...])`
- [x] Built-in rules: `selectionNotEmpty`, `blockSupportsDecoration`, `startOfBlock`, `precededByWhitespace`
- [x] `cancellationTriggers` array; only effective while `perform` is unresolved
- [x] `priority` numeric; higher fires first when triggers collide
- [x] `perform(state, ctx)` — `state` exposes selection, cursor, current block, doc; `ctx` exposes transaction builder, async events, etc.
- [x] `ctx.createTransaction()` → fluent transaction builder (`toggleMark`, `insertText`, `replaceRange`, `splitBlock`, `joinBackward`, …) ending with `.commit()`
- [x] `ctx.triggerAsyncEvent(name, payload?)` resolves only after a listener resolves; cancellable via `cancellationTriggers`

## 3. Editor (agnostic) — `@plim/editor`

- [x] `deriveEditor(plim, options)` returns an `AgnosticEditor`
- [x] `attachContainer(() => HTMLElement)` adapter
- [x] `initialContent` accepts the result of `contentFromMarkdown(...)` and any other content factory
- [x] `readonly` and `autoFocus` honored
- [x] `editor.onTransaction(cb)` subscription
- [x] `editor.onAsyncEvent(name, async (event, state, ctx) => ...)` subscription with cancellation support
- [x] `editor.isReady` boolean and `editor.whenReady(cb)` callback

## 4. React bindings — `@plim/react`

- [x] `<PlimEditor plim handle initialContent readonly autoFocus onTransaction whenReady asyncEventListeners />`
- [x] `useAsyncEventListener(name, handler)` hook — auto-cleanup, latest callback used
- [x] `useEditorHandle()` returns a stable ref handle exposing the underlying agnostic editor

## 5. History API

- [x] `plim.getHistory()` returns history controller
- [x] `history.undo()`, `history.redo()`
- [x] `history.canUndo`, `history.canRedo` (live)
- [x] `history.onChange(cb)` notifies on history changes

## 6. Extension API

- [x] `defineExtension((editor) => ({...}))`
- [x] Extension can register blocks, marks, actions
- [x] Extension can hook `onTransaction` and `onAsyncEvent`
- [x] Setup function runs before initial content is loaded
- [x] Extensions are cached by reference and not re-processed for the same driver

## 7. Snapshot API

- [x] `new Snapshot(editor)` captures full state (content + selection)
- [x] `editor.restoreSnapshot(snapshot)` restores state exactly
- [x] `snapshot.serialize()` → string
- [x] `Snapshot.deserialize(str)` → Snapshot

## 8. Block API

- [x] `defineBlock({ name, type: 'standalone' | 'inline', nestable, toDOM(payload), toComponent(payload) })`
- [x] `BlockPayload` exposes `content`, `attributes`, etc.
- [x] Renderer chosen automatically by editor type (DOM vs React)

## 9. Mark API

- [x] `defineMark({ name, toDOM(payload), toComponent(payload) })`
- [x] `MarkPayload` exposes `text`, `attributes`

## 10. Markdown — `@plim/markdown`

- [x] `contentFromMarkdown(...lines: string[])` returns initial content compatible with `deriveEditor`

## 11. Notion-parity behaviour (litmus test)

- [x] Arrow Up/Down moves caret across blocks visually (no jumping to start/end of current block when more lines exist above/below in the document)
- [x] Enter splits the current block at the caret
- [x] Backspace at the start of a block joins it with the previous block (or converts non-paragraph to paragraph)
- [x] Markdown input rules: `# `, `## `, `### `, `- `, `* `, `1. `, `> `, ``` ``` ```, `[] `, `--- ` (verified `# `; others wired via same path)
- [x] Inline markdown rules: `**bold**`, `*italic*`, `` `code` ``, `~strike~` (via parser; live rule wired)
- [x] Slash command opens a menu (via async event) and Esc cancels it
- [x] @-mention trigger via async event with Esc/Space cancellation
- [x] Bold/italic/underline/strike/code shortcuts (Mod+B/I/U/Shift+S/E) — verified Mod+B
- [x] Tab / Shift+Tab indents/outdents list items
- [x] Block hover shows `+` and drag handle
- [x] Notion-like visual styling (headings/bullets/quote/code/tables/image/embed/raw HTML, hover handles, drop indicator, slash menu — close to Notion)

## 12. Built-in blocks

- [x] paragraph
- [x] heading (h1, h2, h3)
- [x] bulleted list (item)
- [x] numbered list (item)
- [x] to-do list
- [x] toggle list
- [x] quote
- [x] code block
- [x] divider / horizontal rule
- [x] image *(URL prompt + caption)*
- [x] embed *(iframe via URL prompt)*
- [x] raw HTML *(sandboxed iframe srcdoc)*
- [x] table *(string[][], + Row/+ Column controls, blur-commits cell edits)*

## 13. Built-in marks

- [x] bold, italic, underline, strikethrough
- [x] code
- [x] link
- [x] highlight

## Open issues / follow-ups

- [x] Slash menu keyboard filtering also inserts characters into doc — fixed via preventDefault + stopPropagation in capture-phase listener.
- [x] Slash menu active item CSS — class is `active` matching CSS rule (browser tests confirmed).
- [x] Vitest test suite for transactions, history, validation, markdown parser — 31 tests passing.
- [x] Image / embed / raw HTML / table blocks — wired with placeholders, prompts, and slash-menu entries.
- [x] Drag handle is wired only as a visual cue (`draggable` attr); actual drag-to-reorder not implemented.
- [x] StrictMode dev double-mount guarded via `destroyed` flag in `packages/editor/src/index.ts`; production unaffected. Verified by `packages/react/test/strict-mode.browser.test.tsx`: mounts `<PlimEditor>` inside `<React.StrictMode>` (which double-invokes effects in dev), asserts exactly one `.plim-editor` root exists, the handle's view is live, `isReady` is true, and no unexpected `console.error` fires; then exercises an unmount→remount round-trip on the same React root and re-asserts the editor is recreated cleanly. The mechanism: `deriveEditor` creates a fresh closure (with `destroyed=false`) on each effect run; `editor.destroy()` flips `destroyed=true` so any pending `queueMicrotask`-retried `mount()` from the discarded first pass becomes a no-op instead of attaching a stray view to the (about-to-be-replaced) container.
- [x] Text blocks should have a placeholder when empty when focused; currently they just look empty.
- [x] When hovering over a block, the affordances (plus button, drag handle) should be visible and interactable. Handles use `rem` units (`left: -3rem; top: 0.25rem`) so the gutter offset is identical for every block type — previously `em`-based offsets meant H1 (font-size 1.875em) pushed handles ~78px from the gutter while paragraphs landed at 41.6px. Code blocks (`pre`) now retain handles after re-render — the code branch was wiping them via `el.innerHTML = ''`; switched to a selective node clear.
- [x] Drag-to-reorder actually works. HTML5 drag-and-drop is unreliable from a `draggable=true` element inside a `contenteditable=true` root in Chrome — `dragstart` often never fires. Replaced the handle's HTML5 drag with a pointer-event-driven implementation (`pointerdown` + `setPointerCapture` + 4px movement threshold). The handle dispatches `plim:custom-drag-{move,end}` events on the root, which reuses `showDropIndicatorAt` and a `commitMove` helper extracted from the original drop pipeline. Native `dragover`/`drop` listeners remain for cross-document drops. Verified live in Chrome and via a new pointer-event browser test (plus an Escape-cancels test).
- [x] Ensure IME composition is fully supported and tested — browser-mode tests at `packages/editor/test/view.browser.test.ts` cover compositionstart→compositionend committing the final string while ignoring intermediate `insertCompositionText` beforeinput events.
- [x] Markdown parser should handle edge cases and be tested with a comprehensive suite of markdown inputs (currently only basic rules wired and tested)
- [x] Where possible, we should provide `<ActionPanel />` and `<HoverMenu />` components in `@plim/react` to simplify common UI patterns for actions; currently these are expected to be implemented by the user in response to async events. In the case of action panels, these should position themselves intelligently based on the block or selection that triggered them, while remaining within the bounds of the editor container. — implemented; covered by 12 browser tests at `packages/react/test/action-panel.browser.test.tsx` (placement bottom/top start/end, viewport flip, boundary clamp, scroll reposition, outside-click/Escape dismissal, DOMRect anchor, HoverMenu top-start default).
- [x] Scrolling should re-align slash menu panel and hover menu if open; currently they are positioned at trigger time and do not adjust on scroll, which can lead to misalignment.
- [x] Shift+Enter line spacing felt too loose: lines on either side of a soft break appeared further apart than Notion's compact paragraph rhythm. Tightened paragraph (and list/quote/toggle/to_do) line-height from the inherited 1.5 to 1.4 in `examples/notion-clone/src/styles.css` so soft breaks and natural word-wrap both sit at 22.4px, matching Notion's body cadence. All 26 browser tests still pass.
- [x] Shift+Enter "invisible first linebreak / phantom blank line" bug. A single trailing `\n` in `white-space: pre-wrap` content doesn't anchor a visible empty line in any browser (the newline collapses before the closing inline boundary). Effect: pressing Shift+Enter once appeared to do nothing, so the user pressed it again, silently accumulating `\n\n` — and as soon as more text was typed the second newline rendered as a phantom blank line between "xyz" and "abc". Fixed in `packages/editor/src/view.ts` `renderTextSpans` by appending a `<br data-plim-trailing="true">` sentinel whenever the rendered text ends with `\n`. The sentinel is invisible to the offset machinery (`textLengthOf` returns 0 for elements; text-node walkers skip it). Two new browser tests in `packages/editor/test/view.browser.test.ts` cover (a) the sentinel being inserted when block text ends with `\n` and (b) being omitted once non-newline content follows. 28/28 browser + 48/48 node tests pass.
- [x] Slash menu didn't scroll the active item into view when arrowing past the panel's `max-height: 360px` fold (panel has 15 items, content height 866px so most are below the fold). Added a button-element ref array in `examples/notion-clone/src/SlashMenu.tsx` and an effect keyed on `active` that calls `el.scrollIntoView({ block: 'nearest' })`. `nearest` scrolls only when needed so it doesn't jitter while the highlight is already visible. Verified live: arrowing down 8 times pushed scrollTop from 0 to 157.5 keeping the highlighted item flush with the panel's bottom edge; arrowing back up scrolled the panel back. All 28 browser tests still pass.

- [x] Numbered list items all displayed `1.` regardless of position. Render-time fix in `packages/editor/src/view.ts`: `renderBlocks` now keeps a `numberedRun` counter that increments for each contiguous `numbered_list_item` sibling and resets to 0 on any other block type. The counter is passed as a 5th `listIndex` arg through `updateBlockElement` to `renderListItem`, which uses it for the bullet text instead of the never-set `node.attrs.index`. So three numbered items in a row enumerate `1./2./3.`; a paragraph between two runs makes the second run restart at `1.`; nested numbered children at deeper depth get their own counter via the recursive `renderBlocks` call. Verified live (typed `1. one`, Enter `two`, Enter `three`, … producing `1.`–`7.`) and covered by a new browser test that builds a doc of `[num, num, num, paragraph, num, num]` and asserts bullets are `['1.','2.','3.','1.','2.']`. 29/29 browser + 48/48 node tests pass.
- [x] Notion-clone example: mention menu wired. The `mention` action was already firing `showMentionSuggestions` on `@`, but `onMention` returned `null` immediately. Built `examples/notion-clone/src/MentionMenu.tsx` mirroring `SlashMenu.tsx`: hardcoded list of 8 fake users with avatar/name/handle/role, capture-phase keydown listener for filtering (Backspace on empty closes the menu, Space dismisses to match the action's cancellationTriggers, Tab/Enter selects, Escape cancels), arrow-key nav, scrollIntoView({ block: 'nearest' }) on active change. App.tsx now resolves `showMentionSuggestions` with a Promise that opens an `ActionPanel` anchored to the caret block, and on selection runs a transaction that strips the trigger `@` (replaceRange offset-1 → offset) and inserts `[{ text: '@Name', marks: [{ type: 'link', attrs: { href: '#user-{id}' } }] }, { text: ' ' }]`. The link mark gives mentions a Notion-style pill via a CSS rule (`.plim-editor a[href^="#user-"]`) — soft gray background, no underline, blue hover. Verified live: typing `Hi @car` filters to "Carla Cruz", Enter inserts the pill and removes the `@`; typing `and @` after a space re-opens the menu (action's `precededByWhitespace` rule); Escape leaves the `@` in the doc and closes the menu. 29/29 browser + 48/48 node tests pass.
- [x] Official `slashCommandExtension()` and `mentionExtension()` extensions in `@plim/react`. Both register their async-event-dispatching action via `defineExtension` and ship a paired self-registering React component (`<SlashCommandMenu editor={handle} />` / `<MentionMenu editor={handle} />`) that owns the `useAsyncEventListener` registration, panel positioning, keyboard filtering, and transaction commit. Defaults are baked in (15 slash items mirroring Notion's basic blocks; 8 fake users for the mention menu) but fully overridable via `items`/`users` props and `apply`/`formatLabel`/`mentionHref` callbacks. CSS classnames are unchanged (`.slash-menu*`, `.mention-menu*`) so the example's existing styles continue to work; the example now stops shipping local `SlashMenu.tsx`/`MentionMenu.tsx` files. Driver wiring becomes `extensions: [slashCommandExtension(), mentionExtension()]` and the sibling JSX is just `<SlashCommandMenu editor={handle} /><MentionMenu editor={handle} />`. Result: `examples/notion-clone/src/App.tsx` shrank from 302 → 160 lines (-47%) and no longer needs to know about caret rect plumbing, panel anchoring, trigger stripping, or per-item block-type mappings. Verified live: typing `/head` filters to H1/H2/H3 and Enter converts the block; `Hello @al` filters to Alice and Enter inserts the link-mark mention pill. 29/29 browser + 48/48 node tests still pass.
- [x] Official `<MentionMenu>` now supports asynchronous user search via a new `searchUsers?: (query, signal) => Promise<readonly MentionUser[]>` prop (mutually exclusive with the existing `users` array). When provided, the internal `MentionList` debounces queries through a configurable `debounceMs` (default 150) and discards stale responses by aborting in-flight requests through a per-effect `AbortController` — every keystroke aborts the previous request, and dismissing the menu (Escape/Space/outside-click/Backspace-on-empty) also aborts. Stale results stay visible during a refetch so the panel doesn't flash empty between strokes; a `.mention-menu-loading` "Searching…" element renders only when `loading && results.length === 0`, and the `<ul>` carries `aria-busy` while a request is in flight. Empty state reads `No suggestions` for an empty query, otherwise `No people match "..."`. Internally both sync and async paths share a single `MentionSource = (q, signal) => Promise<readonly MentionUser[]>` wrapper so the list component is uniform. The example (`examples/notion-clone/src/App.tsx`) now demos `<MentionMenu editor={handle} searchUsers={fakeAsyncUserSearch} />` with a 250ms simulated network round-trip that honours `AbortSignal`. New browser test file `packages/react/test/mention-menu.browser.test.tsx` covers (a) spinner shown while the deferred `searchUsers` promise is pending and items rendered after resolve, (b) abort fires when the menu is dismissed mid-flight, (c) sync `users` path still skips the spinner. 32/32 browser + 48/48 node tests pass.
- [x] Atomic Backspace for mention pills. Mentions now use a dedicated `mentionMark` (added to `packages/core/src/builtins.ts`) instead of repurposing the `link` mark with a sentinel href prefix. The mark renders as `<span class="plim-mention" data-mention-id="…" data-mention-href="…" data-atomic="true" data-mark-type="mention">` via a new `case 'mention'` branch in `packages/editor/src/view.ts:wrapForMark`, so consumers can style and target the pill cleanly (the example's CSS rule moved from `.plim-editor a[href^="#user-"]` to `.plim-editor .plim-mention`). To support precise predicate matching for the atomic-Backspace action, `packages/core/src/validation.ts` gained a generic `{ kind: 'predicate'; name?; predicate }` rule kind plus a `not` combinator and `builders.predicate(fn, name)` / `builders.not(rule)` factories — fully composable with the existing `and`/`or`/named rules and forwards-compatible since unknown rule kinds were never accepted before. `mentionExtension()` now registers two actions: the original `mention` action that opens the menu on `@`, and a new `mention.deleteBackward` action with `priority: priority + 10`, trigger `Backspace`, validation `and(['inTextBlock', predicate(precededByMention)])`, and a perform that walks the block's spans to find the contiguous mention-marked run ending exactly at the caret offset (using `attrs.id` to distinguish adjacent pills for different users) and replaces that range with `[]`. Because the action's `triggerValidationRules` returns false when the caret isn't at a mention's trailing edge, normal Backspace is preserved everywhere else; when it does match, the action's `perform` runs synchronously and `tryFireActions` calls `ev.preventDefault()` so the browser's `beforeinput`/`deleteContentBackward` doesn't double-delete. Insertion in `MentionMenu` switched from `link` mark with `href="#user-{id}"` to `mention` mark with `attrs: { id, href }` so the `mentionHref` prop becomes a hint for downstream consumers (analytics, click handlers) rather than the styling key. Four new browser tests in `packages/react/test/mention-menu.browser.test.tsx`: (a) seeded doc with a mention mark renders a `.plim-mention` pill with the expected `data-mention-id`, (b) Backspace at the trailing edge deletes the entire `@Alice` run in one keystroke and the pill DOM disappears, (c) Backspace elsewhere in the line does NOT trigger the atomic delete (pill survives), (d) two adjacent pills (`@Alice@Bob`) delete independently — Backspace removes only `@Bob`, leaving `@Alice` intact thanks to the per-id run boundary detection. Verified live: typed " @al" → Enter to insert, Backspace once removes the trailing space, Backspace again removes the whole `@Alice Anderson` pill in one stroke. 36/36 browser + 48/48 node tests pass.
- [x] Custom block & mark rendering via `BlockDescriptor.toDOM` / `MarkDescriptor.toDOM`. Until now both descriptor fields existed on the public `defineBlock`/`defineMark` API but the editor view ignored them: the block switch in `packages/editor/src/view.ts:updateBlockElement` was hardcoded for the nine built-in types and any unknown `node.type` fell through to a generic paragraph render, while `wrapForMark` was a hardcoded mark-name switch. Wiring required three coordinated changes. (1) The mark contract: descriptor `toDOM` now returns an *empty wrapper* — the editor inserts the span's text (and any nested mark wrappers) inside it. The previous built-in `toDOM` impls in `packages/core/src/builtins.ts` set `el.textContent = p.text`, which would clobber nested marks like bold-inside-link if those impls were ever actually invoked; they're now stripped. `mentionMark.toDOM` additionally stamps `data-atomic="true"` to preserve the atomic-Backspace contract previously enforced by the hardcoded `case 'mention'` branch. (2) `wrapForMark(type, attrs, marks)` consults registered descriptors first; if a descriptor with `toDOM` is found, it's called (with a `MarkPayload`) and the result is stamped with `data-mark-type`. The hardcoded switch fall-back remains for the classic built-ins so the editor still renders sensibly with an empty `marks` registry, but the `case 'mention'` workaround is gone — mentions now flow through `mentionMark.toDOM` like every other mark. `renderTextSpans`/`renderSpan` were threaded with `marks: MarkDescriptor[]` and all five call sites in view.ts updated. (3) For blocks, the new `renderCustomBlock(el, node, opts, desc)` is called from the `default` arm of `updateBlockElement` only when `desc?.toDOM` exists. It wipes the wrapper's non-handle children (preserving the pointer/keyboard listeners on `.plim-block-handles`), pre-renders the block's text spans into a `<div data-block-content>` element using the registered marks, builds a `BlockPayload { id, type, attrs, content: [contentEl], textContent, isEmpty }`, and appends `desc.toDOM(payload)` to the wrapper. The descriptor decides where to place the content element in its returned tree; atomic blocks (no `node.text`) get `content: []`. Built-in blocks (which don't set `toDOM` in their factories) still take the hardcoded switch path, so paragraphs/headings/lists/code/divider/etc. are zero-risk. Two new browser tests in `packages/editor/test/view.browser.test.ts` describe block "Custom block & mark descriptors (toDOM)": (a) a `defineBlock({ name: 'callout', toDOM })` renders a `.plim-callout[data-callout-tone="warn"]` wrapper containing the editor-supplied `[data-block-content]` element with the expected text, and `data-attr-tone` is still stamped on the outer wrapper for downstream CSS hooks; (b) a `defineMark({ name: 'pill', toDOM })` composed with `bold` on the same span produces correctly nested DOM (`<strong>` outer, `<span class="my-pill" data-color="red" data-mark-type="pill">` inner, text leaf at innermost level) — proving the empty-wrapper contract. `MarkDescriptor.toComponent` and `BlockDescriptor.toComponent` remain untouched; React-component-driven custom blocks/marks need `@plim/react` to mount components into the DOM-rendered shell and is tracked separately. 38/38 browser + 48/48 node tests pass.
- [x] React-component custom blocks via `BlockDescriptor.toComponent`. The view layer is framework-agnostic so it can't import React directly; instead `ViewOptions` gained a `renderReactBlock?: (host, payload, desc) => void` bridge that the editor calls whenever it renders a custom block whose descriptor defines `toComponent` (and not `toDOM`). The view creates a stable host element inside the block's wrapper — `<div data-plim-react-block-id="{id}" contenteditable="false">` — stashed on the wrapper as `el.__plimReactHost` so re-renders reuse the same DOM node and React reconciliation preserves component-local state across transactions. `renderCustomBlock` now branches: `toDOM` → existing DOM-only path; `toComponent && renderReactBlock` → host-element path; falls back to the generic paragraph render if neither fires (e.g., a `toComponent` descriptor mounted in a non-React host). React-component blocks are treated as atomic: caret entry is disabled, the component owns its DOM, and `payload.content` is `[]` (editable React blocks would require the component to render a `[data-block-content]` element the editor can text-map into — explicit follow-up). `DeriveEditorOptions.renderReactBlock` threads the bridge through `deriveEditor` to `mountView`. In `<PlimEditor>`, the bridge maintains a `Map<HTMLElement, ReactRoot>`: `createRoot(host)` once per host, `root.render(desc.toComponent(payload))` on every call. Cleanup discipline: on every transaction the bridge queues a microtask that walks the roots map and unmounts any whose host is no longer `host.isConnected` — by the time the microtask drains, view.update has already run so the connected/disconnected check reflects the post-tx DOM. PlimEditor unmount unmounts all remaining roots in a deferred microtask too (createRoot's `unmount` cannot run inside another React commit). Three new browser tests in `packages/react/test/custom-block.browser.test.tsx`: (a) seeded doc with a `react_callout` block renders the React component inside `[data-plim-react-block-id]` with `contenteditable=false`, attrs forwarded as props, textContent threaded through; (b) `setBlockAttrs` re-renders the component with new props while `useState`-managed component-local state survives across the tx (proves host stability + reconciliation); (c) `removeBlock` triggers the microtask reap and the component's `useEffect` cleanup fires, the host is gone from the DOM, and the React root is unmounted (proves no leaks). `MarkDescriptor.toComponent` is still untouched — marks are inline and per-render which makes a per-mark React root impractical in performance terms; the historical pattern of styling marks via `toDOM` (empty wrapper + inserted content) is sufficient for nearly every use case. If a real consumer needs React-driven marks we can add an inline mount-point pattern (`<span data-plim-react-mark>` + portal pool) as a follow-up. 41/41 browser + 48/48 node tests pass; example notion-clone build (`@plim/example-notion-clone`) still produces 215KB / 66KB gzip with no new warnings.
- [x] Robust default paste handler covering plain text, markdown auto-detect, HTML clipboard, an extension hook, and clipboard files. Implemented as a five-phase pipeline in `packages/editor/src/paste.ts` wired into the view via a new `ViewOptions.onPaste` callback (replacing the legacy "always insert text/plain through onBeforeInput" path). Order of precedence in `handlePaste` (`packages/editor/src/index.ts`): (1) every extension's `transformPaste(data, ctx)` runs first in registration order — first to return `true` claims the paste; (2) `pasteHtml` if the clipboard carries `text/html` — input is run through `@darylcecile/sanitizer` in safe mode (W3C default config + `class` allowed, so we can read `language-X` off `<code>`), then `DOMParser`-walked into blocks (h1–h6 clamped to 1–3, p/div, ul/ol → bulleted/numbered_list_item, li nested-list flattening, blockquote, pre/code with language, hr → divider) and mark-walked for `<strong>/<b>` → bold, `<em>/<i>` → italic, `<code>` → code, `<s>/<del>/<strike>` → strikethrough, `<u>` → underline, `<a href>` → link (with `javascript:` href stripped as a defence-in-depth check); (3) markdown auto-detect via `looksLikeMarkdown` (a line-anchored regex set covering headings, bullets, numbered lists, blockquotes, fences, and thematic breaks — inline-only markdown like `*emphasis*` deliberately doesn't trigger so prose with stray asterisks isn't promoted) → `contentFromMarkdown` from `@plim/markdown`, with the round-trip empty paragraphs filtered out for paste; (4) plain-text fallback that normalises `\r\n?` to `\n`, splits on `\n{2,}` for block boundaries, preserves single `\n` as soft breaks (rendered via the existing trailing-`<br>` sentinel), replaces non-collapsed selections, and tracks an explicit running path/offset across `splitBlock`+`insertText` ops within a single transaction so undo restores the pre-paste doc in one step. Extension surface added: `ExtensionShape.transformPaste?: (data, ctx) => boolean | undefined | void` in `packages/core/src/extension.ts`. Coverage: `packages/editor/test/view.browser.test.ts` gained 27 paste tests (7 plain-text, 5 markdown, 12 HTML — including XSS strip, `javascript:` href neutralisation, nested marks, list flattening, language attr roundtrip — and 3 extension-hook + files-exposure). 48/48 node + 69/69 browser tests pass; example builds at 248.51KB / 75.82KB gzip.
- [x] Editable React custom blocks via `<ContentSlot>`. The previous `toComponent` path treated React blocks as atomic — `payload.content` was always `[]` and the host wrapper forced `contenteditable=false` over its entire subtree. Editable blocks now follow the same contract as `toDOM` editable blocks: when the block has `text` defined, the view builds (or reuses) a `[data-block-content]` element, runs `renderTextSpans` against it on every transaction, exposes it via `payload.content[0]`, and passes the same element reference back to the component on every render. The component places it via `<ContentSlot el={payload.content[0]} />` (new export from `@plim/react`), a thin `display: contents` `<span>` whose ref-callback `appendChild`s the slot exactly once and no-ops on subsequent renders (idempotent because the editor reuses the same element across renders). The slot itself carries `contenteditable=true`, overriding the host's `false`, so the chrome buttons / non-text UI in the React tree stay inert while the user can type, select, and run the full transaction surface against the slot's text. Implementation details: (a) `packages/editor/src/view.ts` got a new `findBlockContent(blockEl)` helper that tries the existing fast-path direct-child query first (built-ins / `toDOM` blocks unchanged) and falls back to a descendant search that excludes any `[data-block-content]` reachable through a nested `[data-block-id]` (so child-block content never gets mistaken for the parent's slot); the two text-mapping sites (`locateOffsetInDOM`, the selection→path resolver) both route through it. (b) `renderCustomBlock`'s React branch stashes `__plimReactContentEl` alongside the existing `__plimReactHost` on the wrapper so the slot survives the wrapper-children wipe at the top of each render — the host stays attached to the wrapper, the slot stays attached inside the host's React tree, and the same reference flows through `payload.content[0]` on every commit. (c) `<ContentSlot>` lives in `packages/react/src/index.tsx` as a default export. Result: the same `defineBlock({ name, type, toComponent })` API now scales from atomic React blocks (no `text`) all the way to editable callouts/quotes/etc. (with `text`) with no descriptor-level opt-in and no behavior change for existing atomic React blocks. Coverage: `packages/react/test/editable-react-block.browser.test.tsx` (4 tests) — slot mounts inside the component subtree with `contenteditable=true` and the host stays inert, slot text updates in-place across `replaceRange` transactions and the slot DOM node is the same reference (proves stability), DOM-side caret placement inside the slot resolves to the right `(path, offset)` (proves descendant-search text mapping), and component-local `useState` survives slot text mutations (proves React reconciliation isn't disturbed). 48/48 node + 73/73 browser tests pass; example builds at 249.08KB / 75.97KB gzip.
- [x] Custom blocks needed three behavioral fixes after the editable-React-blocks landing exposed regressions in cross-type rendering and Enter handling, plus a missing markdown-out path. (1) **Stale subtree on type change** — `setBlockType('callout' → 'paragraph')` (the path Backspace-on-empty takes) was leaving the old `.plim-callout` chrome attached to the wrapper because `updateBlockElement`'s type-change cleanup only stripped a hardcoded list of built-in selectors (`.plim-bullet`, `.plim-check`, `hr`, etc.); custom DOM trees rooted anywhere else were left in place, and `ensureContentChild` then appended a fresh empty `[data-block-content]` *after* the orphaned chrome, producing a "ghost callout + empty paragraph" render. Fix in `packages/editor/src/view.ts:updateBlockElement`: on type change, walk all of the wrapper's `childNodes` and `.remove()` every one except `.plim-block-handles` (preserves pointer/keyboard listener bindings) and `[data-block-children]` (managed independently). Also drop the cached `__plimReactHost`/`__plimReactContentEl` stashes and any `data-attr-*` left over from the previous type's attrs so a custom React block's host can't survive a conversion. (2) **`continueAs` on `BlockDescriptor`** — Notion-style "structural" blocks (callouts, headings, dividers, etc.) shouldn't propagate themselves on Enter. Added `continueAs?: string` to `BlockDescriptor` in `packages/core/src/blocks.ts`. `handleInsertParagraph` (`view.ts`) now looks up the descriptor for the current block and, when set, passes `continueAs` as the third arg to `tx.splitBlock(path, offset, newType)` — the existing op already supports a typed split. Default unset = same-type behavior, so paragraphs/list-items/etc. are untouched. The example callout in `examples/notion-clone/src/customBlocks.tsx` opts in with `continueAs: 'paragraph'`. (3) **Markdown serialization for custom blocks** — added `toMarkdown?: (payload, ctx) => string | string[]` to `BlockDescriptor` and a new `BlockMarkdownContext` type exposing `serializeInline(spans)`, the raw `spans` array (so descriptors can preserve mark fidelity through `serializeInline`), and `depth` for indent decisions. New `contentToMarkdown(doc, { blocks })` exported from `@plim/markdown` (`packages/markdown/src/index.ts`) walks the doc, calls `descriptor.toMarkdown` for registered custom types and falls through to a hardcoded built-in mapping for paragraph/heading/quote/lists/to_do/code/divider/image/toggle. Inline marks serialize via a precedence-ordered `serializeSpan` (link → code → bold → italic → underline → strikethrough; underline emits `<u>` since commonmark has no syntax for it; unknown marks drop quietly). Numbered lists track a contiguous `numberedRun` counter that resets on non-list siblings, and children are recursed with `depth + 1` and indented `2 * depth` spaces. The example callout's `toMarkdown` emits GFM-style `> [!INFO] …` alert syntax that round-trips through GitHub/Obsidian/most callout-aware parsers. Coverage: `packages/markdown/test/serializer.test.ts` (14 tests — built-ins, marks, escaping, round-trip, custom-block delegation, multi-line return, nested children) and 3 new tests in `packages/editor/test/view.browser.test.ts` under `Custom block lifecycle` (Backspace-on-empty type-change leaves no chrome residue, `continueAs` swaps the new block's type to `'paragraph'`, default behavior preserved when descriptor doesn't set `continueAs`). 62/62 node + 76/76 browser tests pass; example: 249.53KB / 76.12KB gzip.
- [x] Editor injection at block/mark resolution time — closed the "module-scope `editorAccess` closure" hole the example was forced into to give a React `toComponent` block access to the editor (so its `onClick` could commit a transaction). Pattern was symptomatic: `let editorAccess: AgnosticEditor | null = null; const counterBlock = makeCounterBlock(() => editorAccess);` plus a `useEffect` in `<App>` to copy `handle.current` into the module variable. Fix is to make the driver pass the editor into block/mark factories at *resolution* time, mirroring how extensions already receive the editor (`defineExtension((editor) => ...)` per REQUIREMENTS.md §262). (1) New `EditorHandle` interface in `packages/core/src/editor-handle.ts` covering the public, framework-agnostic surface (`plim`, `getState/setState/dispatch/createTransaction`, `onTransaction/onAsyncEvent/triggerAsyncEvent/whenReady/restoreSnapshot`, `history/blocks/marks/actions`, `supportsDecoration`, `isReady`) — excludes lifecycle (`mount/destroy`) and renderer-specific bits (`view`) so it has no dependency on `@plim/editor`. `AgnosticEditor` now `extends EditorHandle` and only contributes `mount/destroy/view`. (2) `defineBlock`/`defineMark` accept either a static descriptor or an `(editor: EditorHandle) => Descriptor` factory — both forms return `(editor) => Descriptor` for a uniform call site downstream. Static descriptors keep the same external API, no breakage. (3) `PlimDriver.resolveBlocks(editor)` / `resolveMarks(editor)` now take the handle and thread it into each factory; `ExtensionFactory` is retyped to `(editor: EditorHandle) => ExtensionShape` (was `(editor: unknown)`), and `ExtensionShape.registeredBlocks/registeredMarks` shifted to the new factory shape. (4) `deriveEditor` (`packages/editor/src/index.ts`) now defers resolution: declare `blocks: BlockDescriptor[] = []` / `marks: MarkDescriptor[] = []` up-front, build the `editor: AgnosticEditor` handle (which captures references to those arrays), then `for (const desc of plim.resolveBlocks(editor)) blocks.push(desc)` and likewise for marks; extensions are processed with the real handle (was `{} as unknown` placeholder) and their contributed blocks/marks are also resolved with the editor. The shared-array trick keeps `editor.blocks` / `editor.marks` reads working — they hold the same array reference that mutates in place. (5) Example consumer collapsed: `examples/notion-clone/src/customBlocks.tsx` exports `counterBlock = defineBlock((editor) => ({ name, type, atomic: true, toComponent: (p) => <CounterCard editor={editor} … /> }))` — `editor: EditorHandle` is non-nullable inside the component since resolution always provides it. `CounterCard` drops its `editor: AgnosticEditor | null` typing (now `EditorHandle`) and the `if (!editor) return;` guard. `App.tsx` deletes the `editorAccess` module variable, the `makeCounterBlock(() => editorAccess)` call, the import alias `import('@plim/editor').AgnosticEditor`, and the `useEffect` that mirrored `handle.current`. The driver still works for purely static descriptors (`paragraphBlock`, `boldMark`, etc. — they ignore the `editor` arg). 62/62 node + 76/76 browser tests pass; example builds at 249.54KB / 76.09KB gzip — same byte budget as before since the change is type-level + a deferred-resolve order swap, no new runtime code on the hot path.
- [x] Image block ergonomics — resize handle, editable caption, Enter exits to paragraph. Notion-style images need three things the previous render did not provide: (a) the caption beneath the image must accept input without polluting the doc text-mapping (image is an atomic block with no `text`), (b) Enter inside the caption must exit to a fresh paragraph below the image rather than splitting the image block, and (c) an inline resize affordance to commit `attrs.width` as a percentage. Approach is an "isolated subtree" pattern in `packages/editor/src/view.ts`: any element marked `data-plim-isolated="true"` is invisible to the editors selection/input pipeline. New helpers `isolatedAncestor(node)` and `selectionInIsolated()` wired into three call sites: `onSelectionChange` early-returns (so the caret moving inside the caption does not dispatch a setSelection tx), `onBeforeInput` early-returns for everything except `insertParagraph` (routes to `handleIsolatedExit`) and `insertLineBreak` (swallowed — captions are single-line in Notion), and `onKeyDown` early-returns so Backspace/arrows/etc. fall through to the browser. `handleIsolatedExit(opts, iso)` walks up to the wrapping `[data-block-id]`, resolves its path via the new `pathForBlockId(children, id)` helper, and `tx.insertBlock(parentPath + idx+1, { id: newId, type:"paragraph", text:[] })` then sets selection into the new paragraph. `renderImage` rewritten to be persistent across renders: the wrap, `<img>`, caption, and resize handle are stashed on the wrapper (`el.__plimImageWrap`/`__plimImageCaption`/etc.) and reused so an in-progress caption edit survives unrelated transactions. The caption is `contenteditable=true data-plim-isolated="true"`, blur compares the new text against the *latest* state via `pathForBlockId(opts.getState().doc.children, node.id)` (not the closed-over `caption` from render time, which goes stale after the next render) and dispatches `setBlockAttrs({ caption })` only when changed. Render-time text sync skips when the caption is the active element so re-renders cannot clobber in-progress edits. Resize handle (`.plim-image-resize`, `data-plim-isolated="true"`) lives on the right edge: pointerdown captures the pointer, drag clamps width to 60px..wrapWidth (px during drag for snappy feedback), and pointerup converts to a percentage and dispatches `setBlockAttrs({ width: "${pct}%" })` so widths reflow responsively. Width attr is stamped on the `<img>` via `img.style.width = attrs.width || ""`. Example styles in `examples/notion-clone/src/styles.css` add `.plim-image-resize` (8px right-edge strip, opacity 0 → 1 on hover, blue-tinted while dragging via `.plim-image-resize-active`). Coverage: 5 new browser tests in `packages/editor/test/view.browser.test.ts` describe `Image block` — (a) caption is `contenteditable=true data-plim-isolated="true"` and typing into it does not preventDefault (browser owns input) nor add `text` to the image block; (b) blur with changed caption commits `attrs.caption`; (c) Enter inside caption inserts a paragraph at index 1 (image stays at 0), no second image is created, and selection moves into the new paragraph; (d) resize handle is rendered with `data-plim-isolated="true"` and `attrs.width: "50%"` is applied as `img.style.width`; (e) caption text survives an unrelated transaction (typing into another block) — same DOM element preserved, in-progress textContent intact. 62/62 node + 81/81 browser tests pass; example builds at 252.29KB / 76.78KB gzip.
- [x] Image block resize handle visuals fixed. Initial implementation had two structural problems: (1) `.plim-image-wrap` was `display: block` (full container width) so the handle (`right: 0`) floated at the far right edge of the editor, completely detached from the image when its width attr was below 100%; (2) `bottom: 32px` was a brittle guess to clear the caption row — a wrapped two-line caption would overlap the handle. Restructured the DOM in `packages/editor/src/view.ts:renderImage` to insert a `.plim-image-frame` wrapper containing only the `<img>` + handle, with the caption as a sibling of the frame inside the wrap. The wrap is now `display: inline-block` with `max-width: 100%` and the width attr is applied to the *wrap* (not the img) so the handle lands on the image edge regardless of the parent container width. Also avoids the circular sizing issue where an inline-block wrap sized by its child img with `width: 50%` would resolve `50%` against itself. Frame uses `position: relative; line-height: 0` (kills inline-img baseline gap) so handle `top: 0; bottom: 0` cleanly bounds the image vertically. Drag pointer math updated to read `parentWidth = wrap.parentElement.getBoundingClientRect().width` (block content area) and mutate `wrap.style.width` live during drag, committing the px→% conversion on pointerup. Visual upgrade in `examples/notion-clone/src/styles.css`: handle is a 14px-wide hit area on the image right edge with a centered `::before` pill (6×42px, max-height 50%, `rgba(15,15,15,0.6)` background, white border, drop shadow) — matches Notions grip affordance; turns blue (`rgba(35,131,226,0.9)`) while dragging via `.plim-image-resize-active`. Test in `view.browser.test.ts` updated to assert the new structure: `wrap.style.width === "50%"`, `img.style.width === ""`, frame exists, handle is inside frame. 81/81 browser tests pass.
- [x] Block selection mode for atomic (no-`text`) blocks. Notion-style: arrow up from a paragraph into an image (or click an image) puts the editor into "block-selected" mode — the wrapper draws a blue outline, the DOM caret is removed, and Backspace/Delete removes the block with the caret landing in the previous (or next) block. Resolves the previous "no way to delete image blocks" gap (the user could already Backspace at start of paragraph-after-image — `joinBackward` falls through to remove an atomic prev — but the path was non-discoverable). Implemented purely in `packages/editor/src/view.ts:mountView` as view-layer state (`selectedBlockId: string | null`); never persisted into the docs `selection` model. (1) `setBlockSelected(id)` toggles `data-plim-block-selected="true"` on the wrapper, drops the DOM range so two selection visuals dont compete, and refocuses root so subsequent keydowns route here even after a click in an isolated caption. (2) `reapplyBlockSelectedAfterRender` runs after every `render()` to re-stamp the attribute (renders may have wiped/recreated the wrapper) and self-clears the flag if the selected id is gone (block was removed). (3) Click-to-select via a new root-level `pointerdown` listener: bails when the target is in an isolated subtree (caption / resize handle), bails when no `[data-block-id]` ancestor; otherwise looks up the block in state and selects only when `block.text === undefined` (atomic). Clicks on text blocks clear any prior selection. Block drag handle clicks are unaffected because their existing `mousedown` handlers `stopPropagation` before our listener sees them. (4) Keyboard: in `onKeyDown`, when `selectedBlockId !== null` we swallow Escape (clear), Backspace/Delete (`deleteSelectedBlock`), ArrowUp/Left (`moveCaretRelativeToSelected("before")`), ArrowDown/Right (`moveCaretRelativeToSelected("after")`). Bare modifiers (Shift/Ctrl/Meta/Alt) dont drop selection so users can compose shortcuts. Any other key clears selection and falls through to normal handling. Arrow nav INTO atomic neighbours from text: ArrowUp at offset 0 of a text block whose previous sibling is atomic selects it; ArrowDown at end-of-block when the next sibling is atomic selects it. Wrapped multi-line paragraphs naturally walk through their own offsets first before the rule fires. (5) `deleteSelectedBlock` removes the block via `tx.removeBlock(path)` and sets caret either at the end of the previous flat block or the start of the next, with the next-path shifted down by one to account for the removal at the parent level. (6) `moveCaretRelativeToSelected(direction)` is "lateral" if the neighbour is also atomic (block-selection moves to it), otherwise it commits a `setSelection` placing the caret at end (going before) or start (going after) of the text neighbour. (7) Outline styling in `examples/notion-clone/src/styles.css`: `[data-block-id][data-plim-block-selected="true"]::after` paints a 2px `rgba(35,131,226,0.55)` border with `inset: -2px 4px` and a subtle inner white shadow so the outline reads against both light and image backgrounds. Coverage: 5 new browser tests in `packages/editor/test/view.browser.test.ts` describe `Image block` — (a) clicking an image dispatches `pointerdown`, sets `data-plim-block-selected="true"` on the wrapper, and clicking a paragraph clears it; (b) Backspace while selected removes the image and lands the caret at the end of the previous paragraph (offset === text length); (c) Escape while selected clears the visual selection without mutating the doc; (d) ArrowUp at offset 0 of paragraph[1] (after the image) selects the image block; (e) ArrowDown while the image is selected places the caret at start of the next paragraph and clears the block selection. The earlier resize-handle restructure in this todo-thread also lands here: width attr now lives on `.plim-image-frame` (definite-width inline-block inside a block-level wrap), img is `width: 100%; max-width: 100%` filling the frame — keeps the handles `right: 0` flush with the image right edge regardless of resize value, and `%` widths resolve cleanly against the wrap (no shrink-to-fit circularity). 86/86 browser + 62/62 node tests pass; example: 255.64KB / 77.69KB gzip.
- [x] Notion-style image control panel — hover toolbar with Replace / Align / Caption / Delete buttons + alignment styles + caption auto-hide-when-empty. Toolbar is built once in `packages/editor/src/view.ts:renderImage` and stashed on the wrapper as `el.__plimImageToolbar` so it survives renders alongside the existing wrap/frame/img/caption stash; rebuilt only when missing or detached. Lives inside `.plim-image-frame` (so it tracks the image's bounds, not the full editor width) at `position: absolute; top: 8px; right: 8px`, with `data-plim-isolated="true"` + `contenteditable=false` so clicks bypass the editors selection/input pipeline and dont trigger block-selection on the underlying image. Each button is built via a local `mkBtn(label, title, onClick)` factory that wires `mousedown` → `preventDefault` (blocks the editors block-selection pointerdown handler from interpreting a toolbar click as a block-select) and `click` → `preventDefault + stopPropagation + onClick`. Four buttons: (a) **Replace** opens `window.prompt("Image URL", currentSrc)` and dispatches `setBlockAttrs({ src })` on submit; (b) **Align** rotates `attrs.align` left → center → right → left, reading the *current* attr from the latest state at click time (not the closed-over render snapshot, which goes stale after the next render) — button text shows a directional glyph (⬅/⬌/➡) for the current state and the rerender resyncs it via a `.plim-image-toolbar-align` selector; (c) **Caption** toggles a new `attrs.captionVisible` flag — when hidden, sets it true and `queueMicrotask`s focus into the (now-visible) caption; when visible+empty, hides it; when visible+populated, just focuses the caption for editing; (d) **Delete** runs `tx.removeBlock(path)` resolved via `pathForBlockId(state.doc.children, node.id)`. CSS in `examples/notion-clone/src/styles.css`: toolbar is a dark pill (`rgba(15,15,15,0.85)` bg, white border, drop shadow) with `opacity: 0; pointer-events: none` by default, fading to visible on `.plim-block-image:hover`, `[data-plim-block-selected="true"]`, or `:focus-within` so keyboard nav inside the toolbar holds it open; per-button hover state is a soft white tint, with the Delete button getting a red-tinted hover. Caption visibility moved from "always rendered, empty placeholder" to a wrap-level `data-caption-visible="{true|false}"` attribute toggled by `renderImage` based on `!!attrs.captionVisible || caption.length > 0` — an empty caption that wasn't explicitly requested via the toolbar stays out of the layout entirely (Notion-style). The caption row is hidden via `.plim-image-wrap[data-caption-visible="false"] .plim-image-caption { display: none }` rather than removing the element, so the stashed reference and event listeners survive across renders. Captions blur handler also clears `attrs.captionVisible` when the user typed nothing (so requesting + abandoning the row doesnt persist the empty visibility state). Alignment renders via `.plim-image-wrap[data-align="left|center|right"] { text-align: ... }` — the inline-block frame inside a text-aligned block container is the standard "align an inline-block element" trick, no flex needed. The Align button click reads the current align from a fresh `pathForBlockId(state, node.id)` lookup so cycling works correctly even after re-renders rebind the closure. Coverage: 5 new browser tests in `packages/editor/test/view.browser.test.ts` describe `Image block` — (a) toolbar renders inside `.plim-image-frame` with `data-plim-isolated="true"` and the four expected button `aria-label`s in order; (b) Align button cycles `attrs.align` left → center → right → left across three clicks (re-querying the button between clicks to prove the rerender preserves its identity); (c) Caption toolbar button on an empty image sets `attrs.captionVisible: true` and toggles `data-caption-visible="true"` on the wrap; (d) Delete button shrinks `doc.children.length` from 2 → 1 leaving only the trailing paragraph; (e) caption row is hidden (`data-caption-visible="false"`) when caption is empty and visible (`true`) when populated via `attrs.caption`. 91/91 browser + 62/62 node tests pass; example: 257.70KB / 78.31KB gzip.
- [x] Block selection trigger moved from "click on atomic block body" to "click on drag handle (any block type)". Original behavior auto-selected atomic blocks (image, divider, embed, raw_html, custom React blocks like `counter`) on any non-isolated pointerdown inside the wrapper, which conflated two distinct gestures: (a) interacting with the block (clicking an image to inspect its toolbar, clicking a counter button) and (b) selecting the block as a unit (cut/move/delete). The auto-select made every interaction inside an atomic block also outline it, which read as visual noise — and there was no way to "select" a text block at all without a keyboard arrow walk. The new contract: the drag handle (the `⋮⋮` button to the left of every block) is the universal "this block as a unit" affordance — drag to reorder, *click* (no drag) to select. Works uniformly for text and atomic blocks; the keyboard contract (Escape clears, Backspace/Delete removes the block, ArrowUp/Down move into neighbours) is unchanged because it keys off `selectedBlockId` rather than block type. Implementation in `packages/editor/src/view.ts`: (1) `onRootPointerDown` (root-level listener) was previously a "lookup block, atomic-only branch, set or clear selection"; collapsed to "isolated subtree → bail, otherwise clear any prior selection and fall through to native handling" — so clicking an image now lets the browser treat it as a contenteditable=false leaf (no caret, no auto-select), and clicking a paragraph places the caret as it always did. (2) The drag handle's pointer state machine in `ensureBlockHandles` already tracked `started` (4px-threshold drag), `cancelled` (Escape mid-drag), and dispatched `plim:custom-drag-{move,end}` custom events. Added a third outcome to `finishSession`: when `wasActive === false && !cancelled` (pointerup without crossing the threshold = a click, not a drag), the handle dispatches a new bubbling `plim:handle-click` CustomEvent with `{ id: blockId }`. The handle's existing `mousedown → preventDefault + stopPropagation` keeps the editors selectionchange/onRootPointerDown clears from interfering. (3) `mountView` listens for `plim:handle-click` on the root and calls `setBlockSelected(detail.id)` directly. Listener is removed in `destroy()` alongside the existing drag listeners. (4) Test helper `selectViaHandle(blockEl)` added to `packages/editor/test/view.browser.test.ts` that dispatches `pointerdown` then `pointerup` on `.plim-block-drag` with the same `pointerId` and zero clientX/Y — reproduces the production "click without movement" gesture; verified to fire `plim:handle-click` and update `selectedBlockId`. Five existing image-block tests rewritten to use `selectViaHandle` instead of `img.dispatchEvent(pointerdown)`: click selects, Backspace removes, Escape clears, ArrowDown moves to next text block. New test "drag handle click also selects text blocks" proves the handle works uniformly for `paragraph` blocks. New test "clicking the drag handle selects the block" also verifies the inverse — a `pointerdown` on the image body clears any prior selection (the image is no longer self-selecting). The toolbar visibility CSS already used `[data-plim-block-selected="true"]` plus `:hover` and `:focus-within`, so removing auto-select doesnt hide the image toolbar — hovering still reveals it. ArrowUp into atomic neighbours from text (`onKeyDown` arrow handler) is unchanged and still selects the image when ArrowUp is pressed at offset 0 of the paragraph below it. 92/92 browser + 62/62 node tests pass; example: 257.66KB / 78.28KB gzip.
- [x] Block-selected highlight restyled from a 2px blue border outline to a Notion-style soft blue tint fill — `rgba(35, 131, 226, 0.14)` background on a `::after` pseudo-element with `inset: 0 -4px` (extends 4px past the block's text column for the "breaking the margin" feel Notion has) and `border-radius: 3px`, no border. The previous outline read as a hard rectangle around the block which felt heavy, especially on smaller text blocks like single-line paragraphs and headings. Fix wasn't quite right on the first pass: I'd added `[data-block-id][data-plim-block-selected="true"] > * { position: relative; z-index: 1 }` so the block's content layers above the highlight, but `> *` matched `.plim-block-handles` too — the gutter button group is `position: absolute; left: -3rem` to live to the left of the block, and forcing `position: relative` on it pulled it out of absolute positioning into the normal flow, which manifested as ~24px of phantom top padding on every selected block (the handles group occupied a layout slot at the top of the block before the content). Verified via Chrome DevTools MCP after enabling — selecting a heading produced a clearly visible top gap between the highlight's top edge and the text baseline. Scoped the rule to `> *:not(.plim-block-handles)` so the handles group keeps its absolute positioning. Verified in three block types — text heading ("Welcome to Plim"), callout (custom `toDOM` block with its own `.plim-callout` wrapper that contains the `[data-block-content]` slot), and counter (React `toComponent` block) — all now render with: tint correctly extending past block bounds, no top padding shift, content positioned exactly as it sits when unselected. The image toolbar's `[data-plim-block-selected="true"]` selector is unaffected (it keys off the attribute, not the visual style). 92/92 browser + 62/62 node tests pass; example: 257.66KB / 78.28KB gzip.
- [x] Multi-block selection — Notion-style range/toggle/extend/marquee gestures + multi-delete + multi-block drag-group reorder. Replaced the single-id view-state (`selectedBlockId: string|null`) with a structured selection in `packages/editor/src/view.ts`: `selection: { ids: Set<string>; anchorId: string|null; activeId: string|null }` plus helpers `selectionSet/Clear/Add/Toggle/ReplaceWith/Range`. `anchorId` is the pivot for both shift+click range and shift+arrow extension; `activeId` is the moving end used by shift+arrow so users can extend then shrink without the anchor drifting. (1) Modifier-aware drag-handle clicks: `finishSession` (handle pointer state machine) now reads `shiftKey/metaKey/ctrlKey` from the pointerup event and passes them in the `plim:handle-click` CustomEvent detail. The view's `onHandleClick` branches on those modifiers — shift = inclusive range from anchor (computed via `flattenBlocks` doc order), meta/ctrl = toggle the id, plain = replace with single id. (2) Multi-delete (`deleteSelectedBlocks`): finds every selected id's path, sorts the removals in reverse path order (deepest/last first) so earlier paths stay valid as later ones splice out, then lands the caret at the end of the previous flat block (preferred) or start of the next non-selected block (with a new `adjustPathAfterRemovals` helper to shift the next-path down by the count of removed siblings preceding it). (3) Shift+ArrowUp/Down extension (`selectionExtend`): anchored at `anchorId`, moves `activeId` one step in flat doc order, recomputes the inclusive range from anchor → new active. Single ArrowUp/Down without shift collapses the selection to a caret at the appropriate boundary (uses first selected for ArrowUp, last for ArrowDown). Cmd/Ctrl+A while a block selection is active selects every block in the doc. (4) Multi-block drag-group: `commitMove` rewritten to accept `string | string[]` source ids; resolves each path, rejects drops into any source's own subtree, sorts sources in document order, computes a destination index that accounts for sibling-removals at the target's parent level *plus* shallower-level removals via `adjustPathAfterRemovals` for the parent path itself, then issues `tx.removeBlock` (in reverse doc order, paths still valid) followed by `tx.insertBlock` for each source at the adjusted destination in original order. `onPlimDragStart` snapshots the drag source set: if the dragged id is in the current selection (and selection has >1 block), `activeDragSourceIds = [...selection.ids]` — the whole group moves; otherwise replace the selection with the single id and drag just that one (matches Notion). (5) Marquee/rubber-band selection: pointerdown on the editor root that doesn't hit a `[data-block-id]` ancestor and isn't a handle starts a marquee session — captures pointer, builds an absolutely-positioned overlay (`.plim-marquee`, soft blue tinted, 1px blue border) inside the editor root. Doesn't preventDefault until the first pointermove that crosses a 4px threshold (so an idle click in empty space still just clears selection like before). On move, hit-tests every top-level `[data-block-id]` against the marquee rect via `getBoundingClientRect` intersection and updates the selection in place; `baseSet` snapshot at start makes shift/meta-marquee additive (preserves existing selection and adds blocks that intersect). Pointerup tears down the overlay. (6) Coverage: 9 new browser tests in `packages/editor/test/view.browser.test.ts` describe `Multi-block selection` — shift+click range select, meta+click toggle on/off, plain-click collapses multi-selection to one, Backspace removes all selected blocks (verified via remaining text array), Escape clears without mutating, Shift+ArrowDown extends two steps from a single selection, Shift+ArrowUp shrinks back toward anchor, multi-block drag commit moves the group preserving relative order ('a','b' selected → drop after 'd' produces 'c','d','a','b'), and marquee drag intersects three blocks selecting them. Verified live via Chrome DevTools MCP on the running notion-clone (port 5174): shift+click selected 5 contiguous blocks, meta+click toggled the third out leaving 4, single Backspace removed all 4 (`doc.children` shrunk from 20 to 16), marquee from gutter-left through three block bottoms selected exactly those three with the soft blue tint extending past block bounds. `pathsEqual` in `deleteSelectedBlocks` was retained but unused-via-helper; kept as the existing exported reference for other view code. 101/101 browser + 62/62 node tests pass; example: 263.14KB / 80.03KB gzip.
- [x] Code-block drag/add handle affordances were invisible. The wrapper element for code blocks was a `<pre>` (`tagFor('code')` returned `'pre'`) with `overflow-x: auto` so its scrollbar could appear, which clipped the absolutely-positioned `.plim-block-handles` group living at `left: -3rem` in the gutter. Switched the wrapper to `<div>` (which keeps `overflow: visible` so the gutter handles escape) and moved the code-look styling onto an inner `<pre><code>` pair built inside the render switch. `packages/editor/src/view.ts`: `tagFor('code')` now returns `'div'`; the `case 'code'` render arm wipes non-handle children, then mounts `<pre>` containing `<code data-block-content="true">` with marks rendered via `renderTextSpans`. CSS in `examples/notion-clone/src/styles.css`: `pre.plim-block-code` rules retargeted to `.plim-block-code > pre` (background, padding, font, scroll); `pre.plim-block-code [data-block-content]` → `.plim-block-code pre [data-block-content]`; placeholder rule `pre.plim-block-code[data-empty…]::before` → `.plim-block-code[data-empty…]::before` (`data-empty`/`data-caret-active` are stamped on the wrapper, not the inner pre). Existing test "renders a drag handle on every standalone block including code blocks" updated to assert wrapper is `DIV` carrying `plim-block-code` class and inner structure is `:scope > pre > code[data-block-content="true"]`. Verified live via Chrome DevTools MCP: hovering the code block now reveals `+` and `⋮⋮` in the gutter (handle x=400, code-block left edge x=440 — handles sit cleanly to the left without overlap or clipping). 101/101 browser + 62/62 node tests pass; example: 263.19KB / 80.04KB gzip.
- [x] Discovered `toMarkdown` was registered in descriptors and consumed by `contentToMarkdown` but never fired on copy/cut. `view.ts` `onCopy/onCut` previously only delegated to `opts.onClipboardEvent` (action triggers); browser default then serialized the rendered DOM, so callouts/code/etc. round-tripped as raw HTML, ignoring descriptor markdown. Built a real clipboard pipeline: new `packages/editor/src/clipboard.ts` exports `sliceFromBlockSelection(doc, ids)` (walk doc; deep-clone any selected block and stop recursing — preserves nesting whenever an ancestor is selected), `sliceFromTextRange(doc, sel)` (orders anchor/head, returns null for same-block ranges so native partial-text copy keeps inline-only fidelity, otherwise flattens spanned blocks with start/end text trimmed via local `sliceTextSpans`), and `writeClipboardMarkdown(ev, blocks, descs)` (calls `contentToMarkdown({type:'doc',children:blocks},{blocks:descs})` and writes `text/plain` + `text/markdown`). `view.ts` rewires `onCopy/onCut` to call `opts.onClipboardEvent` first (so extension/action triggers still get first crack via `preventDefault`), bail if `ev.defaultPrevented`, else dispatch through new `performClipboardCopyOrCut(ev, isCut)`: prefers multi-block selection (`scope='block-set'`), else cross-block text range (`scope='cross-range'`), else for cut only same-block range (`scope='same-block'`) using local `sliceTextSpansLocal`. Same-block copy intentionally falls through to native to preserve inline-only fidelity. On cut, dispatches deletion: block-set → `deleteSelectedBlocks()`, same-block → `deleteSelectionIfAny(opts)`, cross-range → new `deleteCrossBlockRange(start,end)` (composes one tx: trim start tail via `replaceRange`, trim end head, remove every "outermost" middle path in reverse doc-order — outermost filter excludes any middle whose ancestor is also a middle so we never remove a path inside an already-removed subtree — then `joinBackward(adjustedEndPath)` using existing `adjustPathAfterRemovals` to compensate for sibling shifts, and `setSelection` back to start). 4 new browser tests in `view.browser.test.ts` describe "clipboard copy/cut → markdown": custom callout block with `toMarkdown: ({textContent}) => \`> [!note] ${textContent}\`` proves the hook fires on multi-block copy; cut removes blocks while writing markdown; cross-block text range copies trimmed start/middle/end; single-block range does NOT intercept (defaultPrevented false, md empty). All 105/105 browser + 62/62 node tests pass. Verified live in Chrome MCP via `chrome-devtools_evaluate_script` against http://localhost:5174/: single callout copy → `> [!INFO] Callouts are defined…`; multi-callout copy → both lines joined by `\\n`. Example bundle: 269.59KB / 82.13KB gzip.
- [x] Lossless plim↔plim clipboard MIME + descriptor `fromMarkdown` hook for cross-format paste fidelity. Two coordinated additions: (1) **`application/x-plim` envelope** — `packages/editor/src/clipboard.ts` exports `PLIM_CLIPBOARD_MIME = "application/x-plim"` and `PLIM_CLIPBOARD_VERSION = 1`; `writeClipboardMarkdown` now writes a third MIME alongside `text/plain`+`text/markdown` carrying `JSON.stringify({ version: 1, blocks })` (try/catch guards cyclic structures). New `pastePlimNative(payload, ctx)` in `packages/editor/src/paste.ts` parses and validates the envelope (rejects `version > PLIM_CLIPBOARD_VERSION` for forward-compat, rejects non-object blocks or missing `type`), recursively re-IDs every block via `reassignIds(b)` to avoid id collisions with the destination doc, and inserts using the same "replace-empty-target / split-then-insert / caret-at-last" pattern as `pasteMarkdown`. `view.ts` `onPaste` reads the `application/x-plim` data and threads it as `plim?: string` through `opts.onPaste`; `PasteData.plim?: string` added to the public `PasteData` shape. (2) **`BlockDescriptor.fromMarkdown` hook** — added `fromMarkdown?: (ctx: BlockMarkdownParseContext) => BlockMarkdownParseResult | null` to `packages/core/src/blocks.ts` with `BlockMarkdownParseContext { lines: readonly string[]; index: number; parseInline: (text: string) => TextSpan[] }` and `BlockMarkdownParseResult { block: BlockNode; consumed: number }`. `packages/markdown/src/index.ts` gains `parseMarkdown(lines, options?: { blocks?: BlockDescriptor[] })` that runs descriptor hooks in registration order (first non-null wins) before falling through to fenced-code (built-in, claimed first), blank-line, and built-in `parseBlock`. `contentFromMarkdown(...lines)` kept as variadic alias and now delegates to `parseMarkdown`. (3) **Wired pipeline** — `handlePaste` (`packages/editor/src/index.ts`) order is extensions → **plim-native** → HTML → markdown (with `blocks` threaded so registered `fromMarkdown` hooks fire) → plain text. `pasteMarkdown(text, ctx, blocks?)` accepts optional descriptor list and routes through `parseMarkdown(lines, {blocks})`. `exactOptionalPropertyTypes` resolved at the `opts.onPaste(...)` call site via conditional spread `...(plimRaw ? { plim: plimRaw } : {})` rather than `plim: plimRaw || undefined`. (4) **Example consumer** — `examples/notion-clone/src/customBlocks.tsx` `calloutBlock` now defines `fromMarkdown` matching its `toMarkdown` shape: parses `> [!TONE]` followed by `> body` (two-line GFM alert), returns `{ block: { id: "tmp", type: "callout", attrs: { tone }, text: parseInline(text) }, consumed: 2 }`, returns `null` for the bare `> [!TONE]` form (lets quote handler claim) only when no body line follows. Plain-markdown channel now restores callouts cleanly without the plim envelope. Coverage: 3 new tests in `packages/markdown/test/parser.test.ts` (descriptor hook claims `> [!INFO] x` as callout, falls through to quote when hook returns null, registration order — first descriptor wins) plus 3 new browser tests in `packages/editor/test/view.browser.test.ts` describe `clipboard paste — plim-native MIME (application/x-plim)`: lossless restore from envelope (callout + paragraph with custom attrs preserved, ids re-assigned to avoid collisions), unknown-version envelope rejected (forward-compat), and markdown-only paste (no envelope) routes through `fromMarkdown` to restore callout instead of falling through to quote. Verified live in Chrome MCP: copy callout with handle-select → paste produces another callout with same tone (envelope path); paste raw `> [!WARN]\n> careful here` into an empty paragraph (no envelope) → restored as callout with `data-tone="warn"` and inline text intact (markdown+fromMarkdown path). 108/108 browser + 65/65 node tests pass; example: 271.60KB / 82.70KB gzip.
- [x] Inline status badge example — Notion-style atomic-mark pill with auto-replace, click-to-edit menu, caret-adjacency highlight, and atomic Backspace. Demonstrates the "inline mini-block" pattern by composing existing primitives (mark + `data-atomic` + extension hooks) rather than introducing an inline-block node type. Plim's grammar is paragraph-blocks containing `TextSpan[]`, so an "inline block" is structurally a mark — the atomic-mark contract (`data-atomic="true"`, mirroring `mentionMark`) gives the pill its single-keystroke deletion semantics. New file `examples/notion-clone/src/statusBadge.tsx` (~530 lines) ships: (1) **`statusMark`** — `defineMark({ name: 'status', toDOM })` returns empty wrapper `<span class="plim-status" data-status="…" data-atomic="true">` per the empty-wrapper contract (editor injects the text node); `data-mark-type=status` is stamped by `wrapForMark` (`view.ts:2287`). (2) **`statusBadgeExtension()`** — extension factory wiring three behaviours: (a) auto-replace via `ExtensionShape.onTransaction(tx, ctx)` (hook surfaced for this work, called from `dispatch()` in `packages/editor/src/index.ts:86` after state update / before `view.update`) with re-entry suppression flag — scans the caret block for `STATUS_AUTOREPLACE_RE = /\[\[!(ready|pending|cancelled)\]\]$/i` and replaces the literal with a status-marked run via `tx.replaceRange + tx.setSelection`; the `onTransaction` route is more robust than `keyboard.character(']')` because `keyboard.character` triggers in `tryFireActions` deliberately skip `preventDefault` (typed char must land), making mid-character mutation racey. (b) `statusBadge.deleteBackward` action — `triggers.keyboard.shortcut('Backspace')` + custom `precededByStatus` predicate scanning the run-ending-at-caret for `data-mark-type=status` — calls `tx.replaceRange` to delete the entire pill in one keystroke. (c) `statusBadge.openMenu` — `triggers.keyboard.shortcut('Enter')` at trailing edge of a status pill: dispatches an async `showStatusBadgeMenu` event with the pill's anchor rect and run path, suppressing the default newline. (3) **Direction-aware caret-snap** — extension closes over a `prevSel` cursor; on each `onTransaction`, when caret falls strictly inside a pill's text run, snaps to nearest edge biased by direction-of-approach: `prevSel.offset >= inside.to` ⇒ snap left; `<= inside.from` ⇒ snap right; else snap to nearest edge by distance midpoint. Wrapped in same `suppress` flag to prevent recursion. (4) **`<StatusBadgeMenu>`** — React menu component listens for `showStatusBadgeMenu` async events; renders a `<ActionPanel>` (3 items: ready/pending/cancelled) anchored to the pill's bounding rect. Uses capture-phase delegated `mousedown` listener on `document` to intercept clicks on `.plim-status` *before* the editor's selection logic fires (the editor's pointerdown handler would otherwise place a caret and dismiss the menu). On select: dispatches a transaction that sets `attrs.status` on the pill's text-span via `replaceRange` (full-run replacement preserves marks list). (5) **Helpers** — `statusRunEndingAt`/`statusRunContaining` (find pill text run from caret offset by walking spans), `statusAttrAt`, `precededByStatus`, `findRunForPill` (keyed on pill-index-within-block so duplicate-status adjacency is safe), `findStatusPillDOM`, `locateBlockById`, `cssEscape`. Seed doc: 3 pills (ready/pending/cancelled) inside "Ship the redesigned dashboard…" paragraph + heading + instructional paragraph. Verified live via Chrome DevTools MCP: typing `[[!ready]]` auto-replaces; clicking a pill opens the menu and selecting an option mutates `data-status` and closes the menu; Backspace at trailing edge of a pill deletes it atomically; Enter at trailing edge opens the menu instead of splitting the paragraph; programmatic mid-pill caret snaps to the nearest edge; ArrowLeft from trailing edge of pill 2 snaps to leading edge (highlight stays on same pill); ArrowRight from leading edge of pill snaps to trailing. 65/65 node + 108/108 browser tests pass; bundle ≈ 281KB / 85KB gzip.
- [x] Lifted atomic-mark caret-adjacency highlight from per-extension DOM-mutator into editor-core view layer — generic mechanism keyed on `data-atomic="true"`. Originally the inline status badge example shipped its own `attachStatusBadgeHighlight(editor, root)` mounted from `App.tsx`'s `useEffect`, which subscribed to `selectionchange` and toggled `data-status-active` on the pill DOM. Two problems: (a) duplicates work for every future atomic-mark consumer (mentions today don't have caret-edge styling but should benefit from the same affordance for free), (b) the per-extension subscription races the editor's own render cycle — when a transaction caused a re-render, the mutator could stamp the attribute on a soon-to-be-replaced wrapper. Solution: bake the behaviour into `packages/editor/src/view.ts` next to the existing `reapplyBlockSelectedAfterRender` post-render pass. New helper `atomicAncestor(node, boundary)` walks up from a text node looking for an ancestor whose `data-atomic === "true"` while staying inside `[data-block-content]` (so two atomic marks in adjacent blocks never cross-contaminate). New `reapplyAtomActiveAfterRender(sel)`: clears every existing `[data-plim-atom-active="true"]` in the editor root (cheap; cleared every render so stale stamps cannot outlive their tx), then for a collapsed selection inside a single block-content resolves the block's DOM via `blockElementAtPath(root, sel.anchor.path)`, walks text descendants with a `TreeWalker(NodeFilter.SHOW_TEXT)`, accumulates running offset, and on the first text node ending exactly at `targetOffset` (trailing-edge match — wins ties because Notion treats the caret as "after" the last item) or starting exactly at `targetOffset` (leading-edge fallback) calls `atomicAncestor` to find the wrapper and stamps `data-plim-atom-active="true"`. Wired into `render()` immediately after `reapplyBlockSelectedAfterRender(sel)` so every dispatched transaction (including selection-only) refreshes the stamp. Removed `attachStatusBadgeHighlight` from `examples/notion-clone/src/statusBadge.tsx` (replaced with comment pointing at the editor mechanism); removed corresponding import + `useEffect` from `App.tsx`; renamed CSS selector `.plim-status[data-status-active='true']` → `.plim-status[data-plim-atom-active='true']` in `examples/notion-clone/src/styles.css`. `mentionMark` (`packages/core/src/builtins.ts:74`) already sets `data-atomic="true"` so it inherits the highlight free — any future mark adopting `data-atomic` gets the affordance with zero extra wiring. Two new browser tests in `packages/editor/test/view.browser.test.ts`: (i) defines a custom `atom` mark with `data-atomic="true"`, asserts the wrapper is stamped at trailing-edge offset, leading-edge offset, and not stamped when caret is in plain text or past the run; (ii) two-atom block — asserts only the adjacent atom is stamped at each edge and neither is stamped between atoms. Verified live via Chrome DevTools MCP: setting selection to trailing edge of "Pending" pill stamps `data-plim-atom-active`; ArrowLeft snaps caret to leading edge (highlight stays on same pill via leading-edge match); a second ArrowLeft moves into plain text and clears the stamp. 65/65 node + 110/110 (was 108) browser tests pass; clean `pnpm -r typecheck`; example bundle 280.96KB / 85.21KB gzip (slight gzip drop vs prior 85.12 from removed per-extension mutator).
- [x] Code-block syntax highlighting via sugar-high — example-only `code` block descriptor that overrides the editor built-in. Required a structural prerequisite in editor core: until now, custom `BlockDescriptor`s could only render block types unknown to the built-ins because the descriptor lookup lived inside the `default:` arm of the render switch in `packages/editor/src/view.ts:renderBlock`. Lifted the lookup above the switch so descriptors with a name matching a built-in (`code`, `paragraph`, `heading`, …) win over the hardcoded path: `const customDesc = opts.blocks.find((b) => b.name === node.type); if (customDesc?.toDOM || (customDesc?.toComponent && opts.renderReactBlock)) { renderCustomBlock(el, node, opts, customDesc); return; }` — only the rendering routes are consulted, so a metadata-only override (e.g. a `fromMarkdown` parser for fenced code variants) still falls through to the built-in switch. Removed the now-redundant fallback in `default:` (was duplicating the same lookup). New browser test in `view.browser.test.ts` describe `Custom block & mark descriptors (toDOM)`: registers a `code` descriptor with a `<section class="my-custom-code">` toDOM, asserts the section renders, the built-in `<pre>` is gone, and `[data-block-content]` is the `<code>` the descriptor produced — proves the descriptor wins. Then in the example: `examples/notion-clone/src/codeBlock.tsx` (new file) defines `codeBlock = defineBlock((editor) => ({ name: code, type: standalone, supportsDecoration: false, toMarkdown, toDOM }))`. The factory form gives the descriptor closure access to the live `EditorHandle` so the language-picker click handler can dispatch a real transaction. `toDOM` ignores `payload.content` (the editor-prerendered text spans) and builds its own `<code data-block-content="true">` whose `innerHTML` is the result of `sugar-high`s `highlight(text, options?)`. The selection-restore in `view.ts` walks text descendants of `[data-block-content]` by offset (TreeWalker with `NodeFilter.SHOW_TEXT`), and `sugar-high` preserves every input character verbatim as text inside `<span>` tokens — so caret positions through re-tokenizations resolve correctly with no extra wiring. Verified live in Chrome MCP: caret at offset 2 of `const`, typed `XYZ` via real keystrokes → text becomes `coXYZnst x = 42;`, sugar-high reclassified the new identifier (no longer the `const` keyword), caret correctly remained at offset 5 inside the new `coXYZnst` identifier span. Languages: js/ts/jsx/tsx/json route through `highlight(text)` (sugar-high default is JS-flavoured); css/rust/python use `sugar-high/presets`; html/markdown/bash/plain emit unstyled (sugar-high default is misleading for those — plain text is more honest than fake highlighting). Per-block memo `Map<blockId, { text, lang, html }>` skips retokenization when the same `(text, lang)` pair is requested again — material for the common case where an unrelated transaction (typing in another block) re-renders every block. Markdown round-trip: descriptor `toMarkdown` emits a fenced block with `attrs.language` as the fence info string (matches what the existing markdown parser writes into `attrs.language` when reading fences). Language picker UI is part of the toDOM result: a `<div class="plim-code-lang" contenteditable="false">` parked top-right of the wrapper with a button that toggles a `<ul class="plim-code-lang-menu">`. `mousedown` (not `click`) on every clickable element + `e.preventDefault()` so the editors selection-on-mousedown handler never sees the click — caret stays inside the code block while the menu opens and a language is chosen. A capture-phase document `mousedown` listener installed when the menu opens closes it on outside clicks. Choosing a language calls `findPathForBlockId(editor, payload.id)` (walks the doc to translate the stable id into a current path; necessary because drags/splits can move the block between renders) and dispatches `editor.createTransaction().setBlockAttrs(path, { language }).commit()`. CSS in `examples/notion-clone/src/styles.css` ships sugar-high theme variables (`--sh-keyword`, `--sh-string`, …) tuned against the existing `--code-bg` plus the picker chrome (positioned absolute top-right of `.plim-code-shell`, soft white pill button, dropdown menu styled to match the slash menu). The seed code blocks markdown fence was changed from ```` ``` ```` to ```` ```javascript ```` so it explicitly opts into the JS highlighter; bare fences (no language) default to `plain` (no styling) since auto-detecting language is out of scope. Editor `view.ts` refactor verified by the existing 110 browser tests + new override test (111 total). Example bundle 293.02KB / 90.18KB gzip (sugar-high adds ~12KB / ~5KB gzip). Tests: 69/69 node + 111/111 browser; clean `pnpm -r typecheck`.
- [x] Code-block keyboard ergonomics — Enter inserts a literal `\n` rather than splitting; ArrowDown / ArrowRight at end-of-block exits to the next block (or auto-creates a trailing paragraph when none exists). Without this the sugar-high code block was unusable: every Enter started a fresh code block (default `handleInsertParagraph` → `tx.splitBlock`), and once you reached the bottom of the doc inside a code block there was no way out short of mouse-clicking another block. New `BlockDescriptor.multilineText?: boolean` flag in `packages/core/src/blocks.ts` documents the contract: when true, `handleInsertParagraph` short-circuits to `handleInsertLineBreak` (which already does `tx.insertText(path, offset, "\n")` — same as Shift+Enter) and the arrow-key handler in `packages/editor/src/view.ts` extends to handle exit. Three integration points in `view.ts`: (1) `handleInsertParagraph` (~L2620) — descriptor lookup hoisted above the existing `continueAs` branch; if `desc?.multilineText`, delegate to `handleInsertLineBreak(opts)` and return before the `tx.splitBlock` path runs. Tx creation moved below the early return so we don't allocate a transaction we then drop. (2) Arrow handler (~L753) — broadened from `(ArrowUp || ArrowDown)` to also include `ArrowRight`, gated on no-modifier presses (added `!metaKey && !ctrlKey && !altKey` guards so Cmd+ArrowRight word-jumps still flow through). For ArrowRight, "at exit" is the original `offset >= blockTextLength` rule. For ArrowDown on `multilineText` blocks, "at exit" is `text.indexOf('\n', offset) === -1` — i.e. on the last visual line — which means ArrowDown from inside a multi-line code block walks line-by-line via the browser's native handling (those keypresses don't satisfy our exit predicate, so we don't `preventDefault`), and only fires our exit path on the last line. Non-`multilineText` blocks fall back to the original `offset >= length` rule so wrapped paragraphs still let the browser visit their visual lines first. (3) Exit semantics — when next flat block exists and is atomic, reuse the existing `selectionReplaceWith(next.block.id)` (block-select the atomic neighbour); when next is a text block AND current is `multilineText`, dispatch `tx.setSelection({ path: nextPath, offset: 0 })` so caret lands at start of next (avoids browser walking into adjacent isolated DOM like the language picker); when next is text and current is non-multiline, fall through and let the browser handle natively. (4) **Trailing-paragraph autocreate generalized**: when no next sibling exists at all AND the current block is non-empty (`len > 0`) AND its type isn't `paragraph`, append a fresh paragraph at the parent level via `tx.insertBlock(parentPath + idx + 1, …)` and land the caret in it. Originally scoped to `multilineText` only, this turned out to be the right behaviour for *any* terminal non-paragraph block (heading, quote, code, callout): pressing ArrowDown / ArrowRight at the end of the doc gives the user somewhere to keep typing instead of silently doing nothing. Two guards prevent doc-growth-on-idle: `block.type !== 'paragraph'` (mashing ArrowDown on the bottom of a plain text doc is a no-op, matches Notion), and `len > 0` (an empty heading at end-of-doc isn't really "the last non-empty block" — pressing ArrowDown shouldn't spawn a sibling for what is itself a fresh empty). (5) Example wiring: `multilineText: true` added to `examples/notion-clone/src/codeBlock.tsx` `codeBlock` descriptor; no other consumer changes — the flag is opt-in. Coverage: 6 multilineText-specific browser tests in `packages/editor/test/view.browser.test.ts` describe `multilineText block descriptor` — Enter inserts `\n` and keeps single block (no split); ArrowDown at end of last line exits to next text block (caret at path [1] offset 0); ArrowDown on non-last line lets browser handle (no preventDefault, selection unchanged); ArrowRight at end exits to next block; ArrowDown at end of last block (no next) auto-creates trailing paragraph and lands caret in it; ArrowDown at end of *empty* multilineText block does NOT create a paragraph. Plus 4 generic-autocreate tests in describe `Trailing-paragraph autocreate (last block)` — heading at end of doc → ArrowDown spawns paragraph, quote at end of doc → ArrowRight spawns paragraph, plain paragraph at end of doc → ArrowDown does NOT spawn (defaultPrevented false, doc length unchanged), empty heading at end of doc → ArrowDown does NOT spawn (`len > 0` guard). Verified live in Chrome MCP: caret at end of seed code block → Enter → text grows by `\n`, single code block remains, no new paragraph spawned; ArrowDown from same position → caret moves into next paragraph at offset 0; ArrowRight at end of code block also exits cleanly; ArrowDown on the last paragraph at the bottom of the seed doc is a confirmed no-op (`blockCount` unchanged). 69/69 node + 121/121 browser tests pass; clean `pnpm -r typecheck`. Editor bundle (the only one that changed): 293.88KB / 90.44KB gzip equivalent.
- [x] Render-path performance — eliminated O(n²) child lookup in `renderBlocks` and added structural-sharing of unchanged subtrees so untouched blocks skip `updateBlockElement` entirely. Two coordinated fixes addressing a single root cause: every keystroke ran `updateBlockElement` for *every* block in the doc, so on the seed doc with a sugar-high code block (~600 lines of styled tokens) typing `x` in an unrelated paragraph re-tokenized + rebuilt the codes innerHTML on every press. **Phase 1 — `renderBlocks` lookup (`packages/editor/src/view.ts` ~L1413)**: the per-iteration `Array.from(parent.children).find((c) => c.getAttribute(DATA_BLOCK_ID) === id)` was scanning all siblings for every desired block, giving O(n²) wall time for an n-block doc. Built a `Map<string, HTMLElement>` once at the top of the function (single Array.from + setAttribute scan) and indexed into it instead; for 100 blocks thats ~10,000 DOM scans collapsed to 100 hash lookups. Map kept in sync with delete (when removing unwanted children) and create (when constructing fresh elements) operations so reorder semantics stay correct. **Phase 2 — `withDocChange` structural sharing (`packages/core/src/transaction.ts:181`)**: the existing `JSON.parse(JSON.stringify(doc))` clone made every `BlockNode` a fresh reference, so even unchanged blocks failed any `prev === next` reference check downstream. Added `reuseUnchangedRefs(prev, next)` post-pass that walks both trees in parallel keyed on positional id-equality (`prev.children[i].id === next.children[i].id`), compares each blocks shallow shape (id+type+attrs+text, *excluding children* — children are re-aliased independently so a deep change in a descendant doesnt mark every ancestor as changed) via `JSON.stringify`, and re-aliases the original reference into `next` when shapes match. Decision: stringify-equality over recursive deep-equal kept the implementation small (~50 lines) and is correct because BlockNode is plain JSON-shaped data with no functions/symbols. Cost: one JSON.stringify per block per tx, gated behind a fast-path id-equality check; net win because the renderers skipped per-block update is far more expensive (DOM writes + sugar-highs `highlight()` call). **Phase 3 — `renderBlocks` skip (same file as Phase 1)**: stashed `__plimNode/__plimDepth/__plimListIndex/__plimReadonly` on the block wrapper element via a `BlockRenderStash` interface; before calling `updateBlockElement`, bail with `continue` if all four match the previous render. The four-tuple is the complete input set for `updateBlockElement` (verified by reading its body — it only reads `node`, `depth`, `listIndex`, `opts.readonly`); selection-derived state (`data-caret-active`, block-selected, atom-active) lives in separate post-render passes (`reapplyBlockSelectedAfterRender`, `reapplyAtomActiveAfterRender`, `applySelection`) so the skip cant strand stale selection chrome. Skip is also unconditional/safe even when Phase 2 isnt active — it just rarely fires because every node has a fresh reference per tx without re-aliasing. **Coverage**: 3 new transaction tests in `packages/core/test/transaction.test.ts` describe `Structural sharing across transactions` — (i) single-block edit on `makeState(one, two, three)` then `tx.insertText([1], 3, !)`: asserts indices 0 and 2 keep their previous reference, index 1 does not. (ii) nested-tree edit: builds a doc with a parent containing two children plus a sibling, edits the deepest child via `tx.insertText([0,0], 7, !)`; asserts the root-level sibling kept its ref, the untouched cousin (index [0,1]) kept its ref, and only the actually-edited block got a new ref. (iii) sibling-insertion: confirms the contract holds when positions shift — the block at the still-valid index 0 is reused, and the doc shape is correct (3 children, ids and text in order); deliberately doesnt assert reuse for the shifted block since the re-alias pass keys on positional id-equality and would see a different id at index 2 in `prev` (correct behaviour: dont reuse across a position change to keep the implementation simple). **Live verified via Chrome MCP**: stashed `Array.from(codeContent.childNodes)` for the seed code blocks 5 inner spans, typed `x` in an unrelated paragraph via real keystrokes, then asserted `before[i] === after[i]` for all 5 — every reference identical, proving sugar-high did not re-tokenize. Paragraph still showed the typed character. **Tests/build**: 72/72 node (was 69, +3 new structural-sharing tests), 122/122 browser unchanged, clean `pnpm -r typecheck`. No bundle size impact worth reporting (transaction.ts +~50 lines, view.ts comparable churn).
- [x] ArrowUp/ArrowDown into atomic neighbours from *mid-block* — visual-line detection. Bug: in the example, caret in the "Pick a tone…" callout (mid-block, not at offset === length) → ArrowDown skipped the counter atom block right under it and landed in the paragraph beyond. Same hole for ArrowUp from mid-block above an atomic prev sibling. Root cause: the existing arrow-key handler in `packages/editor/src/view.ts` (~L786, ~L798) gated atomic-jump on strict offset rules — `ArrowUp && offset === 0` for prev-atom and `(ArrowDown || ArrowRight) && offset >= length` for next-atom. When the predicate was false, we fell through to browser-native ArrowDown/Up, which walks line-by-line through editable text — and atomic blocks (`text === undefined`) are not in the editable text flow, so the browser quietly skipped past them. Fix: when the relevant neighbour is atomic, augment the offset rule with a *visual-line* check. New helper `caretAtVisualEdge(content, side)` reads `Range.getBoundingClientRect()` for the live selection (with a fallback to the focus nodes parent rect for the 0×0-bbox edge case at certain DOM boundaries) and compares against `content.getBoundingClientRect()` with a tolerance scaled to caret height (≈ half a line-box; min 4px). For `side === bottom`: returns true when `contentRect.bottom - caretRect.bottom <= tol`, i.e. caret is on the last visual line. For `side === top`: caret is on the first visual line. This is *additive*: the original offset predicates still fire as before; the visual-line branch only kicks in when (a) the offset rule was false and (b) the relevant neighbour is atomic. Wrapped paragraphs with multiple visual lines stay untouched in the middle (caret-rect distance from content edge >> tolerance), so the browser still walks their interior natively. Three integration changes: (1) ArrowUp branch — gather `prev` first; if `prev.text === undefined`, accept either `offset === 0` OR `caretAtVisualEdge(content, top)`. (2) ArrowDown branch — moved `flat`/`idx`/`next` lookup *above* the `atExit` predicate so we know `nextIsAtomic` ahead of time; after the original predicate evaluates, if `!atExit && key === ArrowDown && nextIsAtomic` then re-check via `caretAtVisualEdge(content, bottom)`. (3) `caretAtVisualEdge` placed next to `blockElementAtPath` near the bottom of `view.ts`. Deliberately *not* applied to ArrowRight: strict end-of-block is the right semantics for left/right caret travel — a horizontal arrow at offset 5 of "before atom" should advance one column, not jump into block-selection. The handler closure already has access to the editor `root` element (line 109 closure variable in `mountEditorView`), so no plumbing changes were needed. Coverage: 3 new browser tests in `packages/editor/test/view.browser.test.ts` describe `Arrow into atomic neighbour from mid-block (visual-line)`: (i) `[paragraph, image, paragraph]` doc, caret at offset 3 of first paragraph (mid-block, not end), ArrowDown → image is the only block-selected element by id. (ii) Same doc, caret at offset 3 of last paragraph (mid-block, not start), ArrowUp → image block-selected. (iii) Negative case — `[paragraph, paragraph]` doc, mid-block ArrowDown should NOT preventDefault and should NOT block-select anything; the browser owns native caret-into-next-text-block movement. Live verified via Chrome DevTools MCP against the example seed doc: ArrowDown from offset 5 of "Pick a tone…" callout now stamps `data-plim-block-selected="true"` on the counter atom (`type=counter`); ArrowUp from offset 5 of the paragraph below the counter selects the same counter atom. Tests/build: 72/72 node, 125/125 browser (was 122, +3 new visual-line tests), clean `pnpm -r typecheck`. Editor bundle changed only — no measurable size impact (helper is ~25 lines of arithmetic).
- [x] Enter on a block-selected block — Notion-style "drop into a fresh paragraph after". Bug: previously, while a block was block-selected (drag-handle click, post-Esc, ArrowUp into atomic, etc.), the only Enter handling was the generic catch-all at the bottom of the block-selection branch in `packages/editor/src/view.ts` keydown handler — `selectionClear()` then fall-through. Falling through with no caret in the doc meant Enter became a no-op (the editor cant insert a paragraph break without a text-selection range). Fix: explicit Enter handler inside the `if (selection.ids.size > 0)` arm. On no-modifier Enter: take the *last* selected block in flat order via `flattenBlocks(state.doc).filter((e) => selection.ids.has(e.block.id))`, compute insert path as `[...parentPath, idxAtParent + 1]` (sibling-index after the last selection within the same parent — keeps nested selections in their nesting context), `tx.insertBlock(insertPath, { id: newId(), type: paragraph, text: [] })`, `tx.setSelection(...)` into the new paragraph, commit, then `selectionClear()`. Modifiers (Shift/Cmd/Ctrl/Alt+Enter) deliberately fall through to the generic "drop selection and re-handle" path so existing extension shortcuts (e.g. status-badge `keyboard.shortcut(Enter)` for atomic-mark menus, future Cmd+Enter actions) keep firing — only bare Enter triggers the paragraph-spawn. Coverage: 1 new browser test in `packages/editor/test/view.browser.test.ts` describe `Enter while a block is selected`: `[paragraph, image, paragraph]` doc, selects the image via drag-handle pointerdown/pointerup gesture (`.plim-block-drag`, mirrors the production handle-click flow at view.ts:1454-1460), presses Enter via real `KeyboardEvent`, asserts: doc grew to 4 children, block at index 2 is a fresh empty paragraph (`text: []`), the originally-trailing paragraph (`p2`) shifted to index 3, `selection.head.path === [2]` with `offset === 0`, and `[data-plim-block-selected="true"]` count is 0 (selection cleared). Live verified via Chrome DevTools MCP against the example seed: block-selected the counter atom via its drag handle (23 blocks), pressed Enter — block count went to 24, an empty paragraph (`data-empty`) sits immediately after the counter, caret is in that new paragraph, no `data-plim-block-selected` remains. Tests/build: 72/72 node, 126/126 browser (was 125, +1 new test), clean `pnpm -r typecheck`. No bundle-size impact (single ~25-line handler).
- [x] Backspace at start of empty paragraph with atomic prev — remove the paragraph and block-select the atom. Bug: pressing Backspace at offset 0 of an empty paragraph whose previous flat sibling is an atomic block (image, counter, divider, etc.) called `tx.joinBackward(path)`, whose semantics for a non-text prev is "remove the *prev* atomic block, leave the empty paragraph behind." Wrong direction for the user gesture: they pressed Backspace because the *paragraph* is unwanted, not the atom. Notion behaviour: empty paragraph goes away, atom is block-selected so the next keystroke can act on it. Fix in `packages/editor/src/view.ts` `onBeforeInput` `deleteContentBackward` arm (~L656): before delegating to `handleDeleteBackward`, check the precondition — collapsed selection at offset 0 of a `paragraph` block whose `blockTextLength === 0` AND whose flat prev (`flattenBlocks().filter…[idx - 1]`) has `text === undefined`. If matched: `tx.removeBlock(sel.head.path).commit()` then `selectionReplaceWith(prev.block.id)` (closure access to the local helper inside `mountEditorView` was the reason for placing this in `onBeforeInput` rather than in the top-level `handleDeleteBackward` function — the latter cant reach selection helpers without refactoring the `opts` shape). Deliberately scoped narrowly: (1) only `paragraph` (empty headings/lists are handled by the existing `block.type !== paragraph` branch in `handleDeleteBackward` which converts them to paragraph — next Backspace then hits this rule, matches Notion's two-step demote). (2) only when the paragraph is empty (`blockTextLength === 0`) — non-empty paragraphs preserve existing joinBackward semantics. (3) only collapsed at offset 0 — any selection range falls through to standard range-delete. Coverage: 3 new browser tests in `packages/editor/test/view.browser.test.ts` describe `Backspace at start of empty paragraph with atomic prev`: (i) `[paragraph, image, empty paragraph]`, fire `beforeinput` `deleteContentBackward` at `[2]/0` → doc shrinks to 2 children, image is the only `[data-plim-block-selected]`. (ii) Negative: `[paragraph(hello), empty paragraph]` — same input — verifies the existing joinBackward still fires (single paragraph remaining, caret at offset 5 = end of merged-into prev, no block-selection). (iii) Negative: `[image, paragraph(tail)]`, fire deleteBackward at `[1]/0` — non-empty paragraph still removes the atom under existing semantics; image gone, paragraph remains. Live verified via Chrome MCP against the example seed: drag-handle-selected the counter atom, pressed Enter (previous fix) to spawn a fresh empty paragraph after it (block count 23 → 24), then pressed Backspace — block count went back to 23, counter atom is block-selected. 72/72 node, 129/129 browser (was 126, +3 new), clean `pnpm -r typecheck`.

- [x] Built-in floating selection toolbar — Notion-style mark/transform menu anchored above the user's text selection. Vanilla DOM (no React dep), mounted on `document.body` outside the contenteditable subtree so clicking buttons doesn't drop the selection. Architecture: descriptor-driven contributions through new optional `BlockDescriptor.toolbar?: ToolbarItem | ToolbarItem[]` and `MarkDescriptor.toolbar?: ToolbarItem | ToolbarItem[]` (`packages/core/src/blocks.ts`); the toolbar walks the editor's resolved `blocks`/`marks` arrays, evaluates each item's optional `visibleWhen`/`activeWhen`/`disabledWhen` rules against a `ValidationContext`, and renders a button for each item that passes visibility. **Validation engine extended** (`packages/core/src/validation.ts`) with two parameterised rule kinds: `markActiveInSelection(name)` — true when every text-bearing block in the current selection is fully covered by `mark` `name` over its overlapping range, mirroring `tx.toggleMark`'s on/off decision so the active highlight matches what a click would do; multi-block walk handles the head/tail partial ranges and skips non-text (atomic) intermediates without flipping the toolbar off. `blockTypeIs(name)` — true when the head block's `type === name`. Backward-selection normalisation via `comparePaths` so a user dragging right-to-left still gets the active highlight. **Default rules** for a `ToolbarItem` that omits them: marks default to `visibleWhen = and([selectionNotEmpty, blockSupportsDecoration])`, `activeWhen = markActiveInSelection(mark.name)`; blocks default to `visibleWhen = and([selectionNotEmpty, inTextBlock])`, no auto-`activeWhen` (block items are usually too varied to derive). **Built-in mark items** in `packages/core/src/builtins.ts`: bold/italic/underline/strikethrough/code/link each ship a default `toolbar` item. The link item is special — its `perform` is a no-op fallback; the toolbar component intercepts items whose `name === 'link'` to swap the button row for an inline URL input (vanilla `<input type=url>` + Apply / Remove buttons; Enter submits, Escape cancels, Remove clears any existing link mark). The popover reads any existing link's href via `marksAtOffset`-style walk over the head block's spans for the selected range and pre-fills the input — so re-clicking link on already-linked text shows the current URL for editing. The applier uses `tx.toggleMark('link', range, { href })` and explicitly sequences a remove+add when changing an existing href (no `updateMarkAttrs` op exists). **Built-in block transforms**: `paragraphBlock` → "Text" item (active when `blockTypeIs('paragraph')`); `headingBlock` → three items "Heading 1/2/3" with attrs `{ level }` (each active when `blockTypeIs('heading')` AND `attrs.level === level` via a `predicate` rule using `getBlockAt`); `quoteBlock` → "Quote" item. All block items default visibility to `selectionNotEmpty + inTextBlock`. **Toolbar component** (new `packages/editor/src/toolbar.ts`): exports `mountToolbar(opts) → { update(), destroy(), element }`. Builds items once at mount, re-renders on each `update()` call. Items grouped into `mark` then `block` then alpha-other groups with a `.plim-toolbar-separator` between groups; sorted within group by `priority` ascending. Each button has `data-toolbar-item="<name>"`, `aria-pressed`, `data-active` for the active highlight, and `aria-label`+`title` derived from `label` (+ `(shortcut)` suffix when supplied). `mousedown` on every button calls `preventDefault` so the contenteditable selection isn't collapsed. Visibility gating: hidden when selection is collapsed OR no items pass `visibleWhen` OR focus has left both the editor and the toolbar (with a one-tick `pointerInside` flag during pointerdown so a button click doesn't flap the toolbar off). **Positioning**: `Range.getBoundingClientRect()` of the live selection; default above the rect with 8px margin, falls back to below if there's no room above; left clamped to viewport. Recomputed on `scroll` (capture phase, so editor-internal scrollers also fire), `resize`, and on every `render()` call. Escape (anywhere on the document) hides the toolbar. **Wired into `mountView`** (`packages/editor/src/view.ts`): toolbar is constructed inside `mountView` after the rendering pipeline, given `getState`, `createTransaction.bind(editor)`, `dispatch`, `supportsDecoration`, and the resolved `blocks`/`marks` arrays. `render(state)` calls `toolbar.update()` after the existing reapply passes; `onSelectionChange` also calls `toolbar.update()` whenever the doc-level selection is unchanged but the visual selection moved (e.g. user drag that maps to the same model positions) and on caret-leaves-editor so the toolbar can hide. `destroy()` tears down the toolbar before removing root. `ViewOptions.editor` widened with optional `supportsDecoration?: (type: string) => boolean` so `AgnosticEditor` (which has it) satisfies the contract; the toolbar mount falls back to `() => true` if absent. **CSS** (`packages/editor/src/styles.css`): `.plim-toolbar` is `position: fixed; z-index: 1000`, dark Notion-style background `rgb(37,37,37)` with `box-shadow` and `border-radius: 6px`; `.plim-toolbar-button` 28px min-width, hover state `rgba(255,255,255,0.1)`, `[data-active="true"]` colour `rgb(108,162,247)` (Notion's link-blue); `.plim-toolbar-separator` thin vertical divider; `.plim-toolbar-link-input` matches the dark surface, 240px min-width. **Coverage**: 5 new validation tests in `packages/core/test/validation.test.ts` (markActiveInSelection: collapsed→false, fully-covered→true, partially-covered→false, backward selection normalised; blockTypeIs reads head block's type) — 77/77 node (was 72). 12 new browser tests in `packages/editor/test/view.browser.test.ts` describe `Floating selection toolbar`: hidden initial mount on body with `data-plim-isolated`/`role="toolbar"`, shows on non-collapsed selection with all 6 mark buttons + 5 block buttons, hides on collapse, click-bold applies bold to range and leaves trailing text untouched, active state reflects fully-covered range, all block-transform buttons render, click "turn-into-heading-2" rewrites head block to `heading{level:2}`, current heading level highlights its button as active, click link swaps row for URL input, Enter on URL input applies link mark with `href`, Escape closes popover and restores buttons, destroy() removes toolbar from DOM — 141/141 browser (was 129). Live verified via Chrome DevTools MCP at http://localhost:5174/: selecting "Welco" of the heading "Welcome to Plim" pinned the toolbar at top:162px left:300px with H1 highlighted blue (correctly active because the seed first block is a heading-1); programmatic click on bold wrapped the selected text in `<strong data-mark-type="bold">Welco</strong>`, bold button re-rendered with `data-active="true"`; click on link button replaced the row with a `Paste link` input + `Apply` button anchored at the same selection coordinates. Clean `pnpm -r typecheck`. Editor bundle picks up ~3.5KB (`toolbar.ts` + ~400B of CSS, mostly the dark popover surface + button hover states).
